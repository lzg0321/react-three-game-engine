import { Buffers } from '../shared/types';
export declare const generateBuffers: (maxNumberOfDynamicObjects: number) => Buffers;
export declare const useBuffers: (maxNumberOfDynamicObjects: number, debug: string) => Buffers;
